<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.gwtbootstrap3</groupId>
    <artifactId>gwt-bootstrap-modern</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>teavm-bootstrap3-modern</artifactId>
  <name>GWT Bootstrap 3 Modern TeaVM Widget Backend</name>
  <packaging>jar</packaging>
  <description>TeaVM-backed Bootstrap widget API exploration for the separate modern migration track.</description>

  <dependencies>
    <dependency>
      <groupId>org.gwtbootstrap3</groupId>
      <artifactId>gwt-teavm-compat</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-jso</artifactId>
      <version>${teavm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-jso-apis</artifactId>
      <version>${teavm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.teavm</groupId>
      <artifactId>teavm-classlib</artifactId>
      <version>${teavm.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        The Bootstrap 3 widget API is compiled from the GWT module's own sources, so the
        TeaVM build and the GWT build share one definition of every widget rather than
        maintaining a parallel copy. Two groups are excluded:
          * CellTable, DataGrid, ButtonCell and FormPanel - the GWT cell-widget and
            form-submit bridges, which are data-presentation machinery rather than
            Bootstrap widgets;
          * the entry point and the jQuery bridge, which this module re-implements
            against TeaVM's @JSBody under the same package and API.
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <id>add-shared-bootstrap3-sources</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${project.basedir}/../gwt-bootstrap3-modern/src/main/java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>org/gwtbootstrap3/client/ui/gwt/CellTable.java</exclude>
            <exclude>org/gwtbootstrap3/client/ui/gwt/DataGrid.java</exclude>
            <exclude>org/gwtbootstrap3/client/ui/gwt/ButtonCell.java</exclude>
            <exclude>org/gwtbootstrap3/client/ui/gwt/FormPanel.java</exclude>
            <exclude>org/gwtbootstrap3/client/GwtBootstrap3EntryPoint.java</exclude>
            <exclude>org/gwtbootstrap3/client/GwtBootstrap3ClientBundle.java</exclude>
            <exclude>org/gwtbootstrap3/client/shared/js/JQuery.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.teavm</groupId>
        <artifactId>teavm-maven-plugin</artifactId>
        <version>${teavm.version}</version>
        <executions>
          <execution>
            <id>compile-teavm-smoke-app</id>
            <phase>process-classes</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <mainClass>org.gwtbootstrap3.teavm.demo.Bootstrap3ShowcaseApp</mainClass>
              <targetDirectory>${project.build.directory}/teavm</targetDirectory>
              <targetFileName>teavm-bootstrap3-modern-smoke.js</targetFileName>
              <minifying>false</minifying>
              <sourceMapsGenerated>true</sourceMapsGenerated>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
